\* Negative test: the first race in CONCEPT.md 10.8.  gc deletes every
\* manifest whose id does not match the visible object, without first asking
\* whether an upload is still in flight.  TLC must report a violation of I1.
SPECIFICATION Spec

CONSTANTS
    u1 = u1
    u2 = u2
    Uploads     = {u1, u2}
    CleanupMode = "r3"
    GcMode      = "v01"
    RotateMode  = "off"

SYMMETRY Perms

INVARIANTS
    TypeOK
    I1
    I2

CHECK_DEADLOCK FALSE
